FHE-Benchmark ML-Inference — deployment weights (BN folded)
circuit:  fc1 -> x^2 -> fc2   (depth = 2 matvec + 1 square)
layout:   CSV row-major, comma-separated, no header. y = W @ x + b.
acc:      folded test accuracy = 97.96%  (max|logit diff| = 6.104e-05)
input:    flatten 28x28 -> 784, pixels in [0,1] (raw bytes / 255 -- the harness
          already writes them that way), then normalize per pixel: (p - 0.1307) / 0.3081
crop:     center-crop the normalized 28x28 to 22x22 -> 484 (trim 3 px per side) before fc1;
          done in the client right before encryption, so it feeds the full 28x28 (784).
output:   10 logits -> client takes argmax (no softmax needed)
dtype:    float64 in CSV

W1: 128x484   b1: 128
W2: 10x128   b2: 10

test set: the accuracy above was measured at weight-export time (in the training
repo, not this one) on the MNIST 10k test split, read from the raw idx files and
normalized per the `input:` line above.
